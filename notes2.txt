

Dashboard Overview (Top Section)

Add summary cards at the top:

Total Jobs Posted

Total Applicants

Accepted Candidates

Rejected Candidates

Pending Applications

💡 Example:

[ 5 Jobs ]  [ 120 Applicants ]  [ 25 Accepted ]  [ 10 Rejected ]

➡️ This gives quick insights to the company.

2️⃣ Applicant Details View

Right now you only show email.

You should add:

Student Name

College

Branch

CGPA

Resume (View / Download)

Skills

Applied Date

💡 Add a “View Profile” button.

3️⃣ Job Status Control

For each job:

🟢 Open

🔴 Closed

When job is closed:

Students cannot apply

Company cannot accept/reject new applications

4️⃣ Search & Filter Applicants

Add filters:

By CGPA

By Status (Applied / Accepted / Rejected)

By Skills

By College / Branch

👉 Very important for real recruitment.

GOOD-TO-HAVE FEATURES (Medium Priority)
5️⃣ Selection Rounds Tracking

Since you already have Number of Rounds, add:

Round 1: Aptitude

Round 2: Technical

Round 3: HR

Track:

Current round per student

Round-wise status

6️⃣ Notifications

Notify student when:

Application accepted

Application rejected

Selected for next round

(Even simple in-app notification is enough for now)

7️⃣ Edit Job Option

Currently you have delete only.

Add:

✏️ Edit Job
(Change CTC, eligibility, rounds, skills)











28 / 03/2026
student can apply once per job
Implemented Apply button disable logic to prevent multiple applications for the same job.
Developed status-based filtering functionality (APPLIED, SHORTLISTED, SELECTED, REJECTED).

29/ 03/ 2026 
admin profile :  Implemented Proper Company Fetch Logic   users + company_profile → CompanyDTO    Identified mismatch between: users table (3 companies) and company_profile table (1 company)
company profile :  interview status is added 




Failed to load resource: the server responded with a status of 503 (Service Unavailable)Understand this error student:1 Access to XMLHttpRequest at 'http://localhost:8080/api/applications' from origin 'http://localhost:3000' has been blocked by CORS policy: The 'Access-Control-Allow-Origin' header contains multiple values 'http://localhost:3000, *', but only one is allowed.Understand this error :8080/api/applications:1 Failed to load resource: net::ERR_FAILEDUnderstand this error student:1 Access to XMLHttpRequest at 'http://localhost:8080/api/applications' from origin 'http://localhost:3000' has been blocked by CORS policy: The 'Access-Control-Allow-Origin' header contains multiple values 'http://localhost:3000, *', but only one is allowed.Understand this error :8080/api/applications:1 Failed to load resource: net::ERR_FAILED



in student dashboard when i click on apply button it is giving this error and and not applied 
in company dashboard when i click on applicants tab and select any role then the applied student is not showing 
and give me the look like left side panel 